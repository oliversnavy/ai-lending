# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.venv

# Data — raw downloads (large files, not committed)
data/
!data/.gitkeep
!data/processed/.gitkeep

# Results — generated outputs (exclude specific subdirs, not the whole tree)
results/skills/
results/traces/
results/episode_indexes/*.jsonl

# Pilot archive is committed for paper reference; exclude large binary outputs only
results/pilots/**/*.parquet
results/pilots/**/*.pkl

# T0 v1 archive — local reference only, includes multi-hundred-MB traces (some >100MB,
# over GitHub's push limit); not committed
results/archive/

# vLLM build artifacts
vllm-install/

# Jupyter checkpoints
.ipynb_checkpoints/
notebooks/.ipynb_checkpoints/

# Environment
.env
.env.*

# Agent-generated artifacts (written to project root by T1+ agents)
pipeline.py

# OS
.DS_Store
