# Populated after GEPA optimisation run (Treatment 2).
# get_optimised_prompt() will return the locked prompt set.

def get_optimised_prompt() -> str | None:
    """Return GEPA-optimised system prompt, or None if not yet generated."""
    return None
